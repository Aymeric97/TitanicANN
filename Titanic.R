##KaggleTitanic

dtrain <- read.table(file.choose(),header = TRUE,sep=",",dec=".",quote='"',fill=TRUE)
dtest <- read.table(file.choose(), header = TRUE, sep = ",", dec=".",quote='"')
dgs <- read.table(file.choose(), header = TRUE, sep = ",", dec=".",quote='"')

dtrain
dtest
dgs



